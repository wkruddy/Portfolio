# Portfolio — agent guide

This repo uses **project-scoped skills** in `.cursor/skills/` for a learnable multi-agent pipeline.

| Skill | Use for |
|-------|---------|
| `portfolio-orchestrator` | Sequence work, handoffs, user gates |
| `portfolio-research` | Design Brief → `docs/handoffs/research-to-ux.md` |
| `portfolio-ux` | Spec + copy/IA before code |
| `portfolio-frontend` | `ui/` only |
| `portfolio-backend` | `server/` only |

**Start here**: [docs/agent-orchestration.md](docs/agent-orchestration.md) (diagram, checklists, Cursor invocation examples).

**Identity & constraints (all agents)**: [SOUL.md](SOUL.md)

**Research iteration loop**: [docs/research/](docs/research/) (`learnings.md`, `sources.md`)

**Handoff templates**: [docs/handoffs/](docs/handoffs/)

**Lane rule** (when editing `ui/`, `server/`, `docs/`): `.cursor/rules/portfolio-agents.mdc`

Authenticity: Staff++ voice, real work only — no fictional case studies.
